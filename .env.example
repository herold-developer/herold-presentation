# Environment Configuration Example
# Copy this to .env.local and fill in your values
# Do NOT commit .env.local to git

# ============================================================================
# OpenAI TTS (Recommended for audio generation)
# ============================================================================
# Get your API key from: https://platform.openai.com/account/api-keys
# Required for: npm run generate-audio (or --tts-provider openai)
# Cost: ~$0.015 per minute of audio
OPENAI_API_KEY=sk-your-api-key-here

# ============================================================================
# Optional: Alternative TTS Providers (local, no API key needed)
# ============================================================================

# Piper TTS (local, fast, good quality)
# Install: pip install piper-tts
# Download voice: piper --download en_US-lessac-medium
# Usage: npm run generate-audio -- --tts-provider piper

# Coqui TTS (local, flexible)
# Install: pip install TTS
# Usage: npm run generate-audio -- --tts-provider coqui

# Kokoro JS (local, fast, no API keys)
# Install: npm install kokoro-js && node scripts/download-kokoro-models.js
# Usage: npm run generate-audio -- --tts-provider kokoro

# ============================================================================
# Notes
# ============================================================================
# - Audio files (public/audio/*.mp3) are generated locally, not in git
# - .env.local is in .gitignore and will never be committed
# - Only set OPENAI_API_KEY if you use OpenAI for audio generation
# - Always use environment variables, never hardcode keys in code
